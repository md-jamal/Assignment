#include"dlist.h"

dll *insert_begin(dll *head)
{
	dll *new = create_node();      //Create New Node
	
	if(head == NULL)
		head = new;
	else{
		head->prev_node = new;
		p->next = head;
	}
	head = new;
	
	return head;
}
